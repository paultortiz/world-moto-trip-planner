node_modules
.next
tsconfig.tsbuildinfo
# Keep environment variables out of version control
.env

/generated/prisma

# Secrets folder (keep README, ignore everything else)
/secrets/*
!/secrets/README.md
